package day42;

public class OfficeHour {

   // Class is a templet for object
    // it can define what kind of attribute and behaviour an object shoud have

    // attribute:   instance field (variable)
    // behavior:  instance methods

    // Object is the actual thing created out of this template and any object created out of same template will have the
    //attribute and behaviours defined in the template


    //Protecting the data(attribute) of the object we can encapsulate the instance field by making
    // it private and pro   public getters and setters

    // 4 access modifier: public, protected, default, private.

    // public - accessible anywhere
    // default - no access modifier, only within the package, also know as package private
    //private - only acceesible within the same class

    //

}
